<!DOCTYPE html>
<html lang="<%= typeof lang !== 'undefined' ? lang : 'en' %>" dir="<%= typeof direction !== 'undefined' ? direction : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : site.title %></title>
    <meta name="description" content="<%= typeof description !== 'undefined' ? description : site.description %>">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Outfit:wght@700&family=Noto+Naskh+Arabic:wght@400;700&display=swap"
        rel="stylesheet">

    <!-- Styles -->
    <link rel="stylesheet" href="<%= baseurl %>/assets/css/main.css">
</head>

<body>

    <main>
        <%- content %>
    </main>

    <footer style="text-align: center; padding: 40px 0; color: #64748b; font-size: 0.9rem;" dir="ltr">
        <p>&copy; <span id="year"></span> Huda Jassim.</p>
    </footer>

    <script>
        document.getElementById('year').textContent = new Date().getFullYear();
    </script>

    <script src="<%= baseurl %>/assets/js/youtube.js"></script>
</body>

</html>

